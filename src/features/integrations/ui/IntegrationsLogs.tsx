import { FC } from "react";
import { Box, Text } from "@chakra-ui/react";

export const IntegrationsLogs: FC = () => {
  return (
    <Box>
      <Text color="fg.muted">Журнал вызовов — в разработке</Text>
    </Box>
  );
};
